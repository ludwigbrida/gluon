[package]
name = "gluon-wgpu"
version = "0.0.0"
publish = false
edition.workspace = true

[dependencies]
bytemuck = { version = "1.25.2", features = ["derive"] }
gluon-core = { path = "../gluon-core" }
wgpu = "30.0.1"
